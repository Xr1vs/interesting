{
  "name": "audio-prep-console",
  "version": "1.0.0",
  "description": "Browser-based audio prep console for Roblox — pitch shift, noise reduction, chunked export, and optional auto-upload to Roblox. Processing runs client-side; the small server only relays uploads to Roblox's Open Cloud API.",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.2.0"
  }
}
